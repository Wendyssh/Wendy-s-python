2019110711401440 函数
一、函数
1、基本知识
--函数是代码的一种组织形式
--函数应该能完成一项特定的工作，而且一般一个函数值完成一项工作
--有些语言分函数和过程两个概念，通俗解释是有返回结果的叫函数(function)，无返回结果叫过程 (percedure),python不加以区别
--函数的使用
    --先定义，后使用
        ---只定义的话，不会执行
        ---def 后跟一个空格
        ---函数名需要遵循“见名知意”的原则，大驼峰命名只给类用
        ---后面的冒号和括号不能省略，括号内可以有参数
        ---函数内所有代码缩进（tab和空格键不能混用。tab键可以固定使用）

        案例：
        #下面只是函数声明：只是定义没有使用，结果返回最后一个print语句
        def func()
            print("thisisanexample")
            print("ilovethe wolrd")
        print("函数结束了")
    --使用函数，一般叫调用函数
          ---直接写出函数名字，后面小括号不能省略，括号内内容根据情况确定
          func()
          #返回结果和func()后面的两个print函数


2、函数的参数和返回值
--参数：负责给函数传递一些必要的数据或信息
    --形参：在函数定义时用到的函数，没有具体值，只是一个占位符号
    --实参：在调用函数时输入的值
---返回值
    --调用函数时的执行结果
    --使用return 返回结果
    --如果没有值需要返回，推荐使用 return None 表示执行结束
    --如果函数没有return 关键字，则函数默认返回 None

---help可以提供帮助

#九九乘法表 ex_15_func乘法表.py

二、参数详解
1、参数分类
   ---普通参数/位置参数
   ---默认参数
   ---关键字参数
   ---收集参数

